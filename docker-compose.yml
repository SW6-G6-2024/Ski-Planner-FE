version: '0.1'
services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    volumes:
      - ./src:/app
    environment:
      - CHOKIDAR_USEPOLLING=true
    command: npm start